backend:
  name: github
  repo: AutodiffLabs/autodifflabs.github.io
  branch: main
  auth_scope: repo
  client_id: Ov23li8aeOf71Xru85St
  login: true
  base_url: https://decap-proxy.anirudhxplorer.workers.dev/
  auth_endpoint: "/auth"

media_folder: "assets/uploads"
public_folder: "/assets/uploads"

collections:
  - name: "blogs"
    label: "Blog Posts"
    folder: "/blogs"
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Cover Image", name: "image", widget: "image" }
      - { label: "Body", name: "body", widget: "markdown" }

  - name: "docs"
    label: "Docs Pages"
    folder: "/docs"
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Body", name: "body", widget: "markdown" }

  - name: "hypergrad docs"
    label: "Hypergrad Docs"
    folder: "projects/hypergrad/docs"
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Body", name: "body", widget: "markdown" }

  - name: "hypergrad blogs"
    label: "Hypergrad Blogs"
    folder: "projects/hypergrad/blogs"
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Cover Image", name: "image", widget: "image" }
      - { label: "Body", name: "body", widget: "markdown" }