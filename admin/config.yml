backend:
  name: github
  repo: AutodiffLabs/autodifflabs.github.io
  branch: main
  auth_scope: repo
  client_id: Ov23li8aeOf71Xru85St
  login: true
  base_url: https://decap-proxy.anirudhxplorer.workers.dev/
  auth_endpoint: "/auth"

media_folder: "assets/uploads"
public_folder: "/assets/uploads"

collections:
  - name: "blogs"
    label: "Blog Posts"
    folder: "projects/blogs"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Cover Image", name: "image", widget: "image" }
      - { label: "Body", name: "body", widget: "markdown" }

  - name: "docs"
    label: "Docs Pages"
    folder: "projects/docs"
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Body", name: "body", widget: "markdown" }

  - name: "hypergrad_docs"
    label: "Hypergrad Docs"
    folder: "projects/hypergrad_docs"
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Body", name: "body", widget: "markdown" }